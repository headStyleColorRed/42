#include <stdio.h>
int          ft_strlcpy(char *dest, char *src, unsigned int size);

int        main(void)
{
    char a[] = "Hola Rodri";
    char *c = a;
    
    ft_strlcpy(c);
    
    for(int j = 0; b[j] != '\0'; j++)
    {
        printf("%c", b[j]);
    }
    
    return (0);
}
